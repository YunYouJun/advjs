import speechSynthesis from './speechSynthesis';

const state = () => ({});

const mutations = {};

export default {
  namespaced: true,
  state,
  mutations,
  modules: {
    speechSynthesis,
  },
};
