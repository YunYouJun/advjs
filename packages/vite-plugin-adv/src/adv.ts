import type { Plugin } from 'vite'

export function vitePluginAdv(): Plugin {
  return {
    name: 'vite-plugin-adv',
    config() {
      return {
        resolve: {

        },
      }
    },
  }
}
