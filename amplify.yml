version: 1
applications:
  - frontend:
      buildPath: /
      phases:
        preBuild:
          commands:
            - echo "NODE_OPTIONS=$NODE_OPTIONS"
            - node -e "const v8=require('v8');console.log('Heap limit:',v8.getHeapStatistics().heap_size_limit/2**20,'MB')"
            - node --version
            - corepack enable
            - pnpm install --filter @advjs/editor --concurrency 3 --frozen-lockfile
            #- pnpm install --filter @advjs/editor
        build:
          commands:
            - pnpm run editor:build --concurrency 2
      artifacts:
        baseDirectory: editor/core/.amplify-hosting
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: editor/core
  - frontend:
      buildPath: /
      phases:
        preBuild:
          commands:
            - echo "NODE_OPTIONS=$NODE_OPTIONS"
            - node -e "const v8=require('v8');console.log('Heap limit:',v8.getHeapStatistics().heap_size_limit/2**20,'MB')"
            - node --version
            - corepack enable
            # - pnpm install --filter @advjs/playground @advjs/client @advjs/core
            - pnpm install --concurrency 3 --frozen-lockfile
            #- pnpm install
        build:
          commands:
            - pnpm run build:advjs --concurrency 2
            - pnpm -C plugins/plugin-pominis run build --concurrency 2
            - pnpm run build:play --concurrency 2
      artifacts:
        baseDirectory: playground/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: playground
