import { assets } from '@advjs/theme-default'
import type { AdvConfig } from '@advjs/types'

export const audios: AdvConfig['assets']['audios'] = {
  ...assets.audios,
}

export const images: AdvConfig['assets']['images'] = {
  ...assets.images,

  // background
  bbbrustLove: '/img/bg/bbburst-love.svg',
}
