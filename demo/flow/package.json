{
  "name": "@advjs/demo-flow",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "description": "Demo for flow",
  "scripts": {
    "build": "adv build",
    "config": "adv config -m -t public/config",
    "config:base64": "adv config -m -t public/config --base64",
    "dev": "adv --log=info",
    "export": "adv export",
    "preview": "vite preview --host",
    "preview-https": "serve dist"
  },
  "devDependencies": {
    "@advjs/plugin-openai": "workspace:*",
    "@advjs/theme-default": "workspace:*",
    "advjs": "workspace:*",
    "https-localhost": "catalog:",
    "nodemon": "^3.1.10"
  }
}
